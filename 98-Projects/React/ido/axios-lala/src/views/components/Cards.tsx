import { url } from 'inspector';
import React, {useState} from 'react'
import Decks from './Decks';

interface CardsProps{
  newGame:boolean;
  cardsMain:Array<any>
  }

function Cards (props:CardsProps){

 const {newGame} = props;

  return (
    
  <div className='mainTwo-TwoCards'>
    
  </div>
  )
  
}

export default Cards