"use strict";

// exercise 1
var num1 = window.prompt("please enter first Num:");
var num2 = window.prompt("please enter second Num:");

function BiggerNum(num1, num2) {
  if (num1 == num2) {
    return result;
  } else if (num1 > num2) {
    return result;
  } else {
    return result;
  }
}

alert(num1 + " is larger than " + num2);
alert(num1 + " is larger than " + num2);
alert(num1 + " is equal to " + num2);