const galina = {
    english: 90,
    math: 70,
    hebrew: 80,
    phiysics: 90,
}

const guy = {
    english: 90,
    math: 70,
    hebrew: 90,
    phiysics: 90,
}


const nikol = {
    english: 50,
    math: 90,
    hebrew: 70,
    phiysics: 90,
}


const artur = {
    english: 70,
    math: 80,
    hebrew: 50,
    phiysics: 20,
}

function yourgrades(student) {
     
        console.log(` english${  student.english} math${student.math}  hebrew${student.hebrew}phiysics ${student.phiysics} `);
    } 
