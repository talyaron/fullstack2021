const student = {
  Name: 'John',
  lastName: 'Doe',
  Algebra: '100',
  Physics: '85',
  Litrature:'70',
  Sports: '100',
}

console.log (`${student.Name} ${student.lastName}'s grades are:`)
console.log (`Algebra: ${student.Algebra}`) 
console.log (`Physics: ${student.Physics}`) 
console.log (`Litrature: ${student.Litrature}`) 
console.log (`Sports: ${student.Sports}`) 