function BMI(ev){
    ev.preventDefault

    const result= document.getElementsByClassName('result')
    const object={}

    console.dir(object)

    let weight= object.weight;
    let height=object.height;
}