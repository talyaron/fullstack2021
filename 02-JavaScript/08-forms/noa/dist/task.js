function BMI(ev) {
    ev.preventDefault;
    var result = document.getElementsByClassName('result');
    var object = {};
    console.dir(object);
    var weight = object.weight;
    var height = object.height;
}
