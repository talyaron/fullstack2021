
let a:number = 5;
let c:number = 4;


function avg (a:number, c:number):number {
    try {
        
        return avg = (a + c) / 2;
    } catch (error) {
        console.error(error) 
    }

    console.log (`${ The average of the numbers is (avg)}`)

}

