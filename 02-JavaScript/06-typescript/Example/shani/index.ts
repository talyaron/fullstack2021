let m:number=24

let w:number=22

function average( a:number ,b:number ):number {
    try{
        const = avg (a+b)/2
        console.log(`The average of m & w is ${average(m,w)}`);

    }
    catch(error){
       console.error(error);
        return 0;

    }
}

