//second exercise
let thisMonth = ['nan', 'january', 'February', 'march', 'april', 'may', 'june', 'july', 'august', 'september', 'october', 'november', 'december']
function month(num) {

    let result = '$[]'
    let i;
    if (i = num)

        alert(thisMonth[num]);
}
month(num = prompt('enter any number between 1 to 12'));

//In array you can write the months regular, without ["nan"] and when you got number, reduce 1 from the number.
//Also you can use switch ( https://www.w3schools.com/js/js_switch.asp )
