//first exercise
function biggerNum(numA, numB) {
    let result = '';
    if (numA > numB) {
        alert(numA)
    }
    else {
        alert(numB)
    }
}
biggerNum(numA = prompt('entrer the first number'), numB = prompt('entrer the s4econd numbert'));
console.log(biggerNum)

// Also import not forget about equal numbers, (if (numA==numB))