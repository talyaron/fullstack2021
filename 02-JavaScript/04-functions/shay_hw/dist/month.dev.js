"use strict";

//second exercise
var thisMonth = ['nan', 'january', 'February', 'march', 'april', 'may', 'june', 'july', 'august', 'september', 'october', 'november', 'december'];

function month(num) {
  var result = '$[]';
  var i;
  if (i = num) alert(thisMonth[num]);
}

month(num = prompt('enter any number between 1 to 12'));